---
name: rrr
description: Quick session retrospective (alias for /retrospective). Use when user says "rrr", "retro", "session summary", "wrap up session". Do NOT trigger for session orientation (use /recap) or handoff (use /forward).
argument-hint: "[--quick | --deep]"
---

# /rrr — Session Retrospective

This is an alias. Run the `/retrospective` skill.

**If you have a Skill tool**: Use `skill: "retrospective"` with args: `$ARGUMENTS`

**Otherwise**: Read `~/.claude/skills/retrospective/SKILL.md` and follow ALL instructions.

---

ARGUMENTS: $ARGUMENTS
